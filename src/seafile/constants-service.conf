# Authors:
# (C) 2021 Idea an concept by Christian Zengel <christian@sysops.de>
# (C) 2021 Script design and prototype by Markus Helmke <m.helmke@nettwarker.de>
# (C) 2021 Script rework and documentation by Thorsten Spille <thorsten@spille-edv.de>

# This file contains the project constants on service level

# Debian Version, which will be installed
LXC_TEMPLATE_VERSION="debian-11-standard"

# Create sharefs mountpoint
LXC_MP="1"

# Create unprivileged container
LXC_UNPRIVILEGED="1"

# enable nesting feature
LXC_NESTING="1"


# Seafile Admin User
SEAFILE_ADMIN=admin@seafile.local

# Seafile Linux User
SEAFILE_SERVER_USER=seafile

#Seafile install Path
SEAFILE_SERVER_HOME=/opt/seafile

# IP or Domain Seafile Web
IP_OR_DOMAIN=127.0.0.1

# Defines the version number of piler mail archive to install (type in exact version number 9.0.7
SEAFILE_VERSION=9.0.7

# Timezone Seafile
TIME_ZONE=Europe/Berlin

# Build a strong password for the SQL user - could be overwritten with something fixed
MARIA_ROOT_PWD=$"(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 20 | head -n 1)"
MARIA_USER_PWD=$"(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 20 | head -n 1)"

# Vars - Don't touch these unless you really know what you are doing!
SEAFILE_SERVER_PACKAGE=seafile-server_${SEAFILE_VERSION}_x86-64.tar.gz
SEAFILE_SERVER_PACKAGE_URL=https://download.seadrive.org/${SEAFILE_SERVER_PACKAGE}
INSTALLPATH=${SEAFILE_SERVER_HOME}/seafile-server-${SEAFILE_VERSION}/


# Build a strong password for the Unix-Seafile-user - could be overwritten with something fixed
UNIX_USER_SEAFILE_PWD=$"(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 20 | head -n 1)"

# Defines the name from the SQL user
MARIA_DB_USER="seafile"

# Build a strong password for the SQL user - could be overwritten with something fixed

MARIA_ROOT_PWD=$"(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 20 | head -n 1)"
MARIA_USER_PWD=$"(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 20 | head -n 1)"
#!/bin/bash


